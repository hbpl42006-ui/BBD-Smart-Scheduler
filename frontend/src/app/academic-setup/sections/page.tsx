import { CrudPage } from '@/components/CrudPage';
export default function Page(){const fields=[{key:'program',label:'Program',required:true,relation:{endpoint:'programs',label:'name'}},{key:'semester',label:'Semester',required:true,relation:{endpoint:'semesters',label:'name'}},{key:'year',label:'Year',type:'number',required:true,min:1},{key:'name',label:'Section',required:true},{key:'student_strength',label:'Total Students',type:'number',required:true,min:0}];return <CrudPage title="Sections" description="Manage student sections." endpoint="sections" fields={fields} columns={[fields[0],fields[1],fields[2],fields[3],fields[4]]} loadAllRecords/>}

