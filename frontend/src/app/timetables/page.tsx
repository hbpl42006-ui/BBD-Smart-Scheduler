'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {AdminLayout} from '@/components/layout/AdminLayout';import {Header} from '@/components/layout/Header';import {Card,CardContent,CardHeader,CardTitle} from '@/components/ui/card';import {Button} from '@/components/ui/button';import {schedulingApi,type Timetable} from '@/lib/api/scheduling';
export default function Page(){const[data,setData]=useState<Timetable[]>([]),[error,setError]=useState('');useEffect(()=>{schedulingApi.list().then(setData).catch(()=>setError('Unable to load timetables.'))},[]);return <AdminLayout><Header title="Timetables"/><main className="p-5 sm:p-8"><Card><CardHeader><CardTitle>Timetables</CardTitle></CardHeader><CardContent>{error&&<p className="text-red-600">{error}</p>}{!error&&!data.length&&<p className="text-slate-500">No timetables created yet.</p>}<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-3">Title</th><th className="p-3">Session</th><th className="p-3">Semester</th><th className="p-3">Effective date</th><th className="p-3">Actions</th></tr></thead><tbody>{data.map(t=><tr className="border-b" key={t.id}><td className="p-3">{t.title}</td><td className="p-3">{t.academic_session}</td><td className="p-3">{t.semester}</td><td className="p-3">{t.effective_date??'—'}</td><td className="p-3"><Link href={`/timetables/${t.id}/builder`}><Button size="sm">Open Builder</Button></Link></td></tr>)}</tbody></table></div></CardContent></Card></main></AdminLayout>}
